# 月度竞品/零部件技术月报 —— 工作流操作规程（SOP）

> 本文件是整条流水线的操作规程。AI 每次生成月报前必须先读这份文件。
> 流水线四阶段：**收集（脚本）→ 梳理（AI校验分类）→ 汇总（AI分析）→ 输出（月报+飞书文档）**

## 一、目录结构与职责

```
AI1/
├── collect.bat / collect.py        # ① 收集：官方源+垂直媒体采集脚本（每月手动运行）
├── config/sources.yaml             # 信息源白名单（三类来源，可增删）
├── data/raw/{YYYY-MM}/items.json   # ① 的输出：当月原始信息条目（标题+链接+日期+来源分类）
├── templates/monthly_report_template.md  # ④ 输出：月报固定模板
├── reports/{YYYY-MM}/              # ④ 月报归档（往期回顾就在这里）
└── monthly-workflow.md             # 本规程
```

**三类来源及定位**（category 字段）：
- `regulator` / `oem` / `supplier` / `industry`：官方来源，**定论依据**
- `vertical_media`（汽车之家、懂车帝、盖世汽车、第一电动等）：**线索发现 + 新车上市事实与参数库核对口径**

## 二、每月标准操作（用户视角）

1. **采集**：月初双击 `collect.bat`（或直接在 Trae 里说"先运行采集脚本"）。
   输出落在 `data/raw/{当月}/items.json`。个别源失败属正常，AI 阶段会用检索兜底。
2. **生成**：在 Trae 里说一句 **"生成本月月报"**（或 "生成 2026-09 月报"），AI 按第三章规程执行。
3. **审阅**：核对 `reports/{当月}/月报.md`，重点看第五章参考建议是否符合本司诉求，修改后定稿。
4. **交付**：让 AI 将定稿推送到飞书云文档，分享给领导。
5. **回顾往期**：让 AI "回顾历月月报/对比 2026-08 与 2026-09"，AI 会读取 reports/ 下归档做纵向对比。

## 三、AI 生成月报规程（AI 每次执行前必读）

### 阶段① 收集核对
- 读取 `data/raw/{当月}/items.json` 与 `collection_log.json`。
- 对失败/空列表源（含懂车帝、易车等JS渲染站），用 WebSearch 按第五章兜底关键词补充检索。
- 对关键新车与新技术条目，用 WebFetch 打开原文核实参数与发布日期。

### 阶段② 梳理校验（逐条执行，宁缺毋滥）
1. **来源分层校验**：
   - **官方来源**（监管公告、车企/供应商官方新闻稿、权威机构发布）→ 定论依据，直接入正式条目。
   - **垂直媒体**（汽车之家/懂车帝/盖世/第一电动等）→ 线索发现渠道。新车上市、交付量等**事实类信息**可入正文，来源列注明"汽车之家/懂车帝"；**技术参数**优先用官方/认证口径核对，核对不上时可用垂直媒体参数库口径但须标注"媒体口径"；纯技术宣称（如"全球首创XX"）无官方佐证 → 进待验证清单。
   - **自媒体/论坛/无署名转载** → 一律剔除进待验证清单。
2. **事件级归并**：同一事件多源报道（官方+媒体）归并为一条，来源用最高层级渠道链接（官方 > 监管 > 垂直媒体）。
3. **时效校验**：发布日期必须在当月（月末月初事件按发布日归属月份；脚本 45 天窗口多出的内容仅作背景，不入正文）。
4. **完整性校验**：标题不足以定性的，抓原文补参数；仍无实质内容则剔除。
5. **去重**：多源转载同一官方发布，只保留官方原发链接。
6. **分类**：按 14 个专业域打标签（动力经济性 / 轻量化 / 空气动力学与热管理 / 整车安全 / 操纵稳定性与乘坐舒适性 / NVH / 涉水与水管理 / 可靠性与耐久 / 环境友好 / 整车防腐 / EMC / 人机友好HMI / 充放电系统 / CAE仿真）；一款车可多标签。
7. **待验证标记**：有行业传播但无官方佐证的重要信息 → 写入月报第四章"待验证清单"，并在正文中只字不提。

### 阶段③ 汇总分析
- 趋势归纳：跨品牌找共性（同类技术≥2家官方发布即成趋势条目）。
- 竞品对比：新车参数以**认证口径**为最高依据（工信部公告、免征购置税目录、C-NCAP/中保研结果），无认证数据时可用垂直媒体参数库口径并标注"媒体口径"；禁用宣传口径。
- 差距分析：标注各技术点上主流竞品的领先维度与空白点。

### 阶段④ 输出
- 严格按 `templates/monthly_report_template.md` 的结构撰写，保存到 `reports/{当月}/月报.md`。
- 语言要求：面向管理层，短句、定量、无夸饰；每条信息注明官方来源链接。
- 信息不足形成完整报告时：如实压缩篇幅，并向用户列出补充材料需求清单（哪些信息缺、建议从哪类官方渠道补），**不得编造**。
- 推送飞书：定稿后用 lark-markdown / lark-doc 能力创建云文档并返回链接（用户需已登录飞书）。

## 四、质量红线

1. 非官方且非主流垂直媒体的数据**永不**进入月报正文（只能进待验证清单）；垂直媒体口径必须注明来源。
2. 参数必须可溯源：正文每个数字都应能通过来源链接核对；新车核心参数（电池/续航/整备质量）以工信部公告、免征购置税目录等**认证口径**为最高依据。
3. 不使用"全球第一/遥遥领先"等宣传用语，用参数与测试结果说话。
4. 单域无官方动态就写"—"，不凑数。

## 五、维护说明

- 信息源失效：更新 `config/sources.yaml` 对应 URL 后重跑脚本。
- 新增监测品牌/供应商：在 `sources.yaml` 中加一行（name/category/url），category 按第一行说明取值。
- 历史对比：直接让 AI 读取 `reports/` 下多期月报做趋势追踪。
