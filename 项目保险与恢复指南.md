# 项目保险与恢复指南（用户操作手册）

> **这份文档是给你（张齐）自己看的操作卡。**
> 配套文档：《项目交接说明.md》是给 AI 看的技术手册，你不用读它。
> 最后更新：2026-09-20

---

## 一、先记住一句话

> **项目改坏了、电脑出问题了、对话断掉了——都不用慌，永远能恢复。**

因为项目同时存在 **3 份副本 + 1 份说明书**：

| 编号 | 副本/说明书 | 存在哪 | 防什么 |
|---|---|---|---|
| ① | 工作版 `webapp/` | 本机 | 日常使用 |
| ② | 稳定版快照 `versions/webapp-v1.0-20260920/` | 本机 | 改坏回滚 |
| ③ | 云端仓库（**全项目**：网页、脚本、快照、采集数据与报告） | Gitee + GitHub | 电脑坏/丢/换机 |
| ④ | 《项目交接说明.md》 | 本机 + 两个云端 | 新对话的 AI 看不懂 |

---

## 二、途径一：本地快照恢复（最常用，30 秒）

> ### 备份文件到底保存在哪？（一句话答案）
>
> 双击 **`一键备份.bat`** 后，备份保存在**本机项目目录下的 `versions\` 文件夹里**，完整路径：
>
> ```
> C:\Users\chunsheng\Desktop\S59\AI场景\AI1\versions\webapp-snapshot-备份日期-时间\
> ```
>
> 例如 `webapp-snapshot-20260920-095400` 就是 2026-09-20 09:54:00 备份的。文件夹里是 webapp 的完整副本（index.html、css、js、img 全在）。
>
> - 备份只存在**本机这块硬盘**，防"改坏"，不防"电脑坏/丢"——后者靠云端（第三节）
> - 想给备份起名字：命令行运行 `powershell -File backup.ps1 十月改版前`，会生成 `versions\webapp-十月改版前\`
> - 保底版 `versions\webapp-v1.0-20260920\` 永不删除；其他 snapshot 确认没用后可手动删（不影响网页运行）
> - 每次备份只复制 webapp 网页目录（约 10MB），不占多少空间

**适用场景**：AI 改完代码后网页白屏、报错、功能不对，想退回"昨天还好好的版本"。

### 操作步骤

1. 关掉服务窗口（黑色/蓝色的那个 bat 窗口直接点 ×），或双击 **`停止网页服务.bat`**
2. 打开文件夹 `C:\Users\chunsheng\Desktop\S59\AI场景\AI1\versions\`
3. 里面有若干 `webapp-xxx` 文件夹：
   - `webapp-v1.0-20260920` = 2026-09-20 确认可用的雏形（**保底版，永远别删**）
   - `webapp-snapshot-日期时间` = 你历次双击"一键备份"存的版本
4. 选一个要恢复的版本，**进入文件夹，全选里面的内容（index.html、css、js、img）复制**
5. 回到 `C:\Users\chunsheng\Desktop\S59\AI场景\AI1\webapp\`，**粘贴，选"替换目标中的文件"**
6. 双击 **`启动网页服务.bat`**
7. 浏览器打开 http://127.0.0.1:8765/ 验证

### 日常预防（重要）

**每次让 AI 做大改动之前，先双击 `一键备份.bat`**，窗口里显示 `[OK] Snapshot created` 就完成了，按回车关闭。这样 versions 里会多一个带时间的快照，随时能退回到改动前。

---

## 三、途径二：云端仓库恢复（电脑坏/丢/换机时用，10 分钟）

**适用场景**：电脑重装、硬盘坏了、换电脑，本地文件全没了。

云端有**两份完全相同的代码**（Gitee 国内访问快，GitHub 作双保险），快照和交接文档也都在里面。

### 3.1 先确认云端文件还在

浏览器登录后打开，能看到文件列表即正常：
- Gitee：https://gitee.com/tsubakiou/competitor-intel
- GitHub：https://github.com/zq114514/competitor-intel

### 3.2 新电脑上的恢复步骤

1. 在新电脑装 Python（勾选 "Add Python to PATH"）：https://www.python.org/downloads/
2. 从 Gitee 下载代码：仓库页面绿色 **「克隆/下载」→「下载 ZIP」**，解压到 `C:\Users\你的用户名\Desktop\AI1\`（路径随意，中文名也可以，脚本已兼容）
3. 双击 `启动网页服务.bat` → 浏览器 http://127.0.0.1:8765/ 即恢复
4. 想要某个历史版本：仓库页面点 **「提交」/「Commits」** → 选历史记录 → 浏览文件 → 下载

> 注意：ZIP 下载不含 `.venv`（Python 依赖库），但本网页只用 Python 自带的 http.server，**不需要装任何第三方库**，直接能跑。

### 3.3 以后一键同步到云端（Gitee / GitHub 都支持）

项目根目录有 **3 个双击即用的同步脚本**，不需要安装 git：

| 双击哪个 | 作用 | 何时用 |
|---|---|---|
| **`一键双推云端.bat`** | 先推 Gitee、再推 GitHub，**推荐日常用这个** | 每月更新完数据、每次备份后 |
| `deploy-gitee.bat` | 只推 Gitee（国内快） | 网络不好/只需国内备份时 |
| `deploy-github.bat` | 只推 GitHub | 只需国外备份时 |

**操作步骤（30 秒）**：

1. 双击对应 bat
2. 按窗口提示**粘贴令牌**（Gitee 一个、GitHub 一个；双推就粘两次），回车
3. 看滚动结果：
   - `NEW` = 云端没有、刚上传
   - `UPDATED` = 云端有旧版、已更新
   - 没变化的文件**自动跳过**（按文件内容比对，不会重复上传、不产生垃圾提交）
   - `FAIL=0` 即成功，按回车关窗

同步范围是**整个项目**：webapp 网页、versions 快照、采集数据 data/、月报 reports/、模板、以及全部 bat/ps1 脚本和 md 文档（`.venv` 依赖库和快捷方式不上云）。所以新机下载 ZIP 后**连备份脚本、启动脚本都在**，完整恢复。

> **安全规则**：令牌只在运行时粘贴，不写进任何脚本、不落盘。早期的 deploy-to-github.ps1 曾把令牌写死在文件里，该文件已从本地和云端删除（详见第五节安全提醒）。

---

## 四、途径三：开启新对话，让 AI 秒接手（最重要）

**适用场景**：对话太长被截断、上下文丢失、开了新对话、对话报错卡死。

### 你只需要做一件事

**新对话的第一句话，原样复制这句发出去：**

```
请先完整阅读项目根目录的《项目交接说明.md》（c:\Users\chunsheng\Desktop\S59\AI场景\AI1\项目交接说明.md），
读完后向我复述：项目结构、启动方式、26维度7板块规则、当前部署状态，确认理解后我们再开始。
```

### 为什么这样就够了

1. 《项目交接说明.md》里写清了：项目是什么、每个文件干什么、数据格式、业务规则、部署地址、踩过的坑、AI 接手清单
2. 这份文档同时存在本机和两个云端仓库，永远丢不了
3. 我还有跨会话项目记忆，同项目的新对话会自动带出历史背景，与文档互为双保险

### 如果连本地文档都没了（换机/重装后）

先按途径二的 3.2 下载 ZIP，文档在 ZIP 根目录里；或者让新对话的 AI 直接读云端：
```
请用 WebFetch 读取 https://gitee.com/tsubakiou/competitor-intel/raw/master/项目交接说明.md，
理解项目背景后再开始工作。
```

### 给新 AI 提需求的小技巧

- 大改动前加一句：**"动手前先双击一键备份（或先让我备份），改完递增缓存版本号并浏览器验证无报错"**
- 涉及数据修改时加一句：**"严格按交接文档第六节业务规则，没有官方来源的标待验证，不许编数字"**

---

## 五、关键凭证与地址清单（账号/令牌丢了看这里）

### 账号
| 平台 | 账号 | 用途 |
|---|---|---|
| Gitee（码云） | 用户名 `tsubakiou` | 国内代码仓库（主用） |
| GitHub | 用户名 `zq114514` | 国外代码仓库（备份） |
| Netlify | 用注册邮箱登录 | 国际托管（国内需代理，备用） |
| 飞书开放平台 | 你的飞书账号 | 自建应用"特性集成AI月报" |

### 令牌（Token）重新生成方法
> 令牌是密码，**不会、也不应该写在任何项目文件里**，只在双击同步脚本时临时粘贴。失效或换电脑时重新生成：
- **Gitee 令牌**：登录 → https://gitee.com/profile/personal_access_tokens → 生成新令牌 → 只勾 `projects` 权限 → 复制。`deploy-gitee.bat` 或 `一键双推云端.bat` 运行时粘贴
- **GitHub 令牌**：https://github.com/settings/tokens/new → 勾 `repo` → Generate token。`deploy-github.bat` 或 `一键双推云端.bat` 运行时粘贴

> ### ⚠️ 安全提醒（2026-09-20，建议尽快做一次）
>
> 早期脚本 `deploy-to-github.ps1` 曾把 GitHub 令牌写死在文件中，且该文件一度被推送到 Gitee 公开仓库。**文件本身已从本地和云端删除，但 git 提交历史里仍留有痕迹**。出于安全考虑：
>
> 1. 请打开 https://github.com/settings/tokens ，**吊销（Delete/Revoke）旧令牌 `ghp_smk…`**
> 2. 重新生成一个新令牌（勾 `repo`），以后只用新令牌
> 3. Gitee 令牌从未写入过任何文件，无需更换

### 公网/访问地址
| 地址 | 说明 |
|---|---|
| http://127.0.0.1:8765/ | 本机访问（bat 启动后有效） |
| http://10.4.149.98:8765/ | 公司局域网访问（IP 可能因换网变化，用 `ipconfig` 重查；依赖本机开机） |
| https://gitee.com/tsubakiou/competitor-intel | Gitee 仓库 |
| https://github.com/zq114514/competitor-intel | GitHub 仓库 |
| https://gleeful-begonia-e78e26.netlify.app/ | Netlify 托管（国内需代理） |

### 飞书应用
- 应用名：**特性集成AI月报**（App ID：cli_aa2649eda4f95cd8）
- 管理后台：https://open.feishu.cn/app
- 已发布 1.0.1，可用范围 4 人（张齐/范苡达/张世坤/李越）
- 主页 URL 换托管地址后，需在后台改 URL → 升版本号 → 重新发布

---

## 六、常见故障对照表

| 故障现象 | 走哪条路 | 一句话操作 |
|---|---|---|
| 网页白屏/点了没反应/改完不对 | 途径一 | versions 快照覆盖 webapp，重启 bat |
| bat 双击一闪而过 | 途径一前置 | 确认 `run-server.ps1` 还在（不在就从 versions 快照里找根目录脚本，或云端下载） |
| 端口被占用/打不开 8765 | 重启 | 双击「停止网页服务.bat」再启动 |
| 电脑坏了/换电脑 | 途径二 | Gitee 下载 ZIP → 装 Python → 启动 |
| 误删文件 | 途径一或二 | 从快照或云端仓库复制回来 |
| 新对话 AI 不了解项目 | 途径三 | 发第四节那句开场白 |
| 网页更新了但看到旧内容 | 清缓存 | 让 AI 递增 index.html 的 `?v=` 版本号，或浏览器 Ctrl+F5 强刷 |
| 同步云端报 FAIL / 401 / 403 | 令牌 | 令牌过期或粘错，按第五节重新生成再跑一次（已传成功的文件不会重复传） |
| 同步时报 406 / 400 | 网络限流 | 等 1-2 分钟重新双击 bat，脚本会跳过已一致的文件、只补传失败的 |
| 飞书里打不开月报 | 查链路 | 本机 bat 是否在跑？网络是否在公司内网？主页 URL 是否正确？ |

---

## 七、每月例行维护节奏（建议固化）

1. **月初改数据前**：双击 `一键备份.bat`（备份存在 `versions\webapp-snapshot-日期时间\`）
2. **改月报数据**：让 AI 改 data.js（或网页编辑模式导出）
3. **本地验证**：启动 bat，浏览器确认无报错
4. **再备一次 + 双推云端**：双击 `一键备份.bat`，然后双击 **`一键双推云端.bat`**（依次粘贴 Gitee、GitHub 令牌；只传有变化的文件）
5. 每 3 个月检查一次 Gitee/GitHub 能否正常登录、令牌是否过期（令牌生成时可选有效期，建议选 90 天）

---

## 八、版本台账（每次重大节点手动记一行）

| 日期 | 版本 | 说明 | 存档位置 |
|---|---|---|---|
| 2026-09-20 | v1.0 | 9月月报雏形（26维度7板块、10车14技术、飞书应用1.0.1） | versions/webapp-v1.0-20260920 + Gitee/GitHub |
| 2026-09-20 | 保险升级 | 新增一键双推（Gitee+GitHub 全项目同步、内容比对跳过、令牌不落盘）；删除含明文令牌的旧脚本 | sync-cloud.ps1 + 一键双推云端.bat + deploy-github.bat + 两云端 |
| （示例）2026-10 | v1.1 | 10月月报 | （届时新增快照并双推） |
| | | | |

---

**本文件自身也已同步到 Gitee 和 GitHub，随项目永存。**
