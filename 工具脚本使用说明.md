# 工具脚本使用说明

> 本文件说明项目全部 .bat / .ps1 脚本的用途和使用方法。
> 规则定义见《特性维度标注准则.md》第8章。

---

## 一、本地网页服务

### 1. 启动网页服务.bat

- **用途**：启动本地网页服务器，端口 8765
- **调用**：`run-server.ps1`
- **操作**：双击运行，出现绿色提示后保持窗口不关
- **访问地址**：
  - 本机：`http://localhost:8765/`
  - 局域网：`http://10.4.149.98:8765/`
- **停止**：关闭命令行窗口，或双击"停止网页服务.bat"
- **注意**：
  - 服务器根目录是 `webapp/`，直接访问 `http://localhost:8765/` 即可
  - 不要访问 `http://localhost:8765/webapp/index.html`（会 404 空白）
  - 改过 js/css 后，浏览器需 Ctrl+F5 硬刷新或清缓存

### 2. 启动网页服务（后台）.bat

- **用途**：后台启动服务器，可关闭命令行窗口
- **调用**：`启动服务.ps1`
- **操作**：双击运行，窗口自动关闭，服务在后台运行
- **停止**：双击"停止网页服务.bat"

### 3. 停止网页服务.bat

- **用途**：停止本地网页服务器
- **操作**：双击运行，自动 taskkill python 进程

---

## 二、版本备份

### 4. 一键备份.bat

- **用途**：备份 webapp/ 目录到 `versions/webapp-snapshot-时间戳/`
- **调用**：`backup.ps1`
- **操作**：双击运行，自动完成
- **使用时机**：大改之前（如改 data.js 结构、改 app.js 渲染逻辑）双击备份
- **恢复**：手动复制快照覆盖 webapp/

---

## 三、云端同步

### 5. 一键双推云端.bat

- **用途**：一次性推送全部文件到 Gitee + GitHub
- **调用**：`sync-cloud.ps1 -Platform all`
- **操作**：
  1. 双击运行
  2. 粘贴 Gitee token（获取：https://gitee.com/profile/personal_access_tokens，勾选 projects）
  3. 粘贴 GitHub token（获取：https://github.com/settings/tokens/new，勾选 repo）
  4. 等待同步完成
- **同步范围**：全项目文件（排除 .venv/ *.lnk *.log deploy_tmp/ bitable-*.json __pycache__/）
- **机制**：按 git blob SHA1 比对，跳过未变文件；webapp/ 文件同时镜像到仓库根
- **推送前必须确认**：
  - data.js 语法正确（括号平衡）
  - index.html 缓存版本号已递增
  - 图片文件存在且非零字节

### 6. deploy-gitee.bat

- **用途**：仅推送 Gitee
- **调用**：`sync-cloud.ps1 -Platform gitee`
- **操作**：双击运行，输入 Gitee token

### 7. deploy-github.bat

- **用途**：仅推送 GitHub
- **调用**：`sync-cloud.ps1 -Platform github`
- **操作**：双击运行，输入 GitHub token

---

## 四、数据采集

### 8. collect.bat

- **用途**：运行数据采集脚本
- **调用**：`prepare.ps1`
- **操作**：双击运行

---

## 五、常见问题

### 浏览器空白页

| 排查步骤 | 检查 | 解决 |
|---|---|---|
| 1 | URL 是否为 `http://localhost:8765/` | 不要加 `/webapp/index.html` |
| 2 | 服务器是否在运行 | 双击"启动网页服务.bat" |
| 3 | F12 → Console 有无 JS 错误 | 按 F12 查看控制台报错 |
| 4 | 缓存版本号是否递增 | Ctrl+F5 硬刷新，或 Ctrl+Shift+Delete 清缓存 |
| 5 | data.js 括号是否平衡 | 运行 Python 校验脚本 |

### 推送后线上页面空白

| 排查步骤 | 检查 |
|---|---|
| 1 | 本地 `http://localhost:8765/` 是否正常？ |
| 2 | GitHub Pages 构建状态（可能需要等 1-2 分钟） |
| 3 | Gitee Pages 是否已开启（需实名认证） |
| 4 | 推送时是否有文件因编码损坏（尤其图片） |

### bat 闪退

| 原因 | 解决 |
|---|---|
| bat 文件含中文字符 | 确保 bat 为纯 ASCII，中文字符移到 ps1 |
| ps1 含中文字面量且无 BOM | ps1 保存为 UTF-8 with BOM，或避免中文字面量 |
| PowerShell 执行策略限制 | bat 中用 `-ExecutionPolicy Bypass` |

---

## 六、Token 安全

- Token 仅在运行时通过键盘输入，**不写入任何文件**
- Gitee token 获取：https://gitee.com/profile/personal_access_tokens（勾选 projects）
- GitHub token 获取：https://github.com/settings/tokens/new（勾选 repo）
- 旧令牌如已泄露到历史文件，立即在平台吊销重新生成
