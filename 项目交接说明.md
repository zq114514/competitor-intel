# 竞品技术情报站 · 项目交接说明

> **给新对话的 AI：开始任何修改前，先完整读一遍本文件。**
> 本文件最后更新：2026-09-20（对应稳定版快照 `versions/webapp-v1.0-20260920`）
> 用户偏好：中文沟通；迭代式完善；所有数据须有官方/权威来源；改动前先讨论给选项，用户确认后再执行。
> 配套文档：《项目保险与恢复指南.md》是**给用户本人**看的故障应急/新对话开场白操作卡；涉及"怎么恢复/怎么备份/新对话怎么让我接手"类问题时引用它，不要让用户读本技术手册。

---

## 一、项目是什么

面向公司管理层的**月度竞品技术情报站**，纯静态网页（免构建、无后端），按月展示：
- 当月**新车型**（CARS，9月10款）：卡片、筛选、详情弹窗（11类性能参数块）
- 当月**零部件/整车新技术动态**（TECHS，9月14条）：卡片、26维度两级筛选、详情
- **总览页**：统计卡、当月新车速览（两列）、重点车型轮播、技术动态速览
- **竞品对比**：COMP_CARS 竞品参数库 + 分组 USP
- 支持**月份切换**（2026-06 ~ 2026-09，每月一个独立 data 文件）
- 网页内**编辑模式**（点"✏️编辑模式"可改文案，改完"📦导出data.js"）

另有数据采集流水线（collect.py + config/sources.yaml + reports/），与网页是**两个独立部分**，规程见 `monthly-workflow.md`。

## 二、快速开始

```
双击  启动网页服务.bat        → 本地服务 http://127.0.0.1:8765/（窗口不能关）
双击  停止网页服务.bat        → 停服务
双击  一键备份.bat            → 大改前给 webapp 打快照到 versions/
双击  一键双推云端.bat        → 全项目同步到 Gitee+GitHub（依次粘贴两个 token，推荐）
双击  deploy-gitee.bat        → 只推 Gitee（需粘贴有效 token）
双击  deploy-github.bat       → 只推 GitHub（需粘贴有效 token）
```

> 云端同步统一走 `sync-cloud.ps1`：同步**整个项目**（排除 .venv/*.lnk/deploy_tmp/bitable-*.json），按 git blob SHA1 比对，未变化跳过；Gitee 新增 POST/更新 PUT，GitHub 一律 PUT。token 只允许运行时参数传入，**严禁写入任何文件**（曾有 deploy-to-github.ps1 硬编码 token，已删除）。

- bat 必须保持**纯 ASCII**（中文在 cmd GBK 下会乱码导致闪退）；真正逻辑全在对应 .ps1 里（PowerShell 5，英文输出）
- 服务工作目录是 `webapp/`，绑定 `0.0.0.0:8765`（局域网可访问）；本机防火墙已放行 8765
- 路由 hash：`#/overview`（总览）、`#/cars`（新车型）、`#/tech`（新技术）、`#/compare`（竞品对比）——**注意是 tech 不是 techs**

## 三、目录结构

```
AI1/
├─ webapp/                        ★网页本体（唯一需要部署的目录）
│  ├─ index.html                  入口；底部 <script> 带 ?v=缓存版本号
│  ├─ css/style.css               全部样式（暗色主题、板块色、chip、卡片、弹窗）
│  ├─ js/
│  │  ├─ data.js                  当月数据（2026-09）：DOMAINS/CARS/TECHS/...
│  │  ├─ data-202606/07/08.js     往期月份数据（同样结构，月份不同）
│  │  └─ app.js                   全部渲染/交互逻辑（无框架，原生 JS）
│  └─ img/                        19 张车型实拍 jpg（来源：汽车之家图库）
├─ versions/                      ★版本快照（只增不改，回滚用）
│  └─ webapp-v1.0-20260920/       2026-09-20 确认可用的雏形稳定版
├─ 启动网页服务.bat / run-server.ps1     本地服务（ps1 自动清理 8765 占用）
├─ 停止网页服务.bat
├─ 一键备份.bat / backup.ps1            快照工具：versions/webapp-snapshot-时间戳/
├─ sync-cloud.ps1                       云端同步核心（Gitee+GitHub，SHA1 比对，全项目）
├─ 一键双推云端.bat / deploy-gitee.bat / deploy-github.bat  同步启动器（运行时粘 token）
├─ collect.bat / collect.py       数据采集流水线（见 monthly-workflow.md）
├─ config/ data/ reports/ templates/      采集流水线的配置/原始数据/月报归档/模板
├─ monthly-workflow.md            采集→校验→分析→出月报 的 SOP
└─ 项目交接说明.md                 本文件
```

## 四、app.js 关键结构（改代码先看这里）

- 全局状态 `state`：reportMonth（当前月）、powerFilter/segFilter/dimGroupFilter/dimFilter（车型页4行筛选器）、techGroupFilter/techFilter（技术页）、projectId
- `navigate()`（369行）：按 hash 路由；`render()` 分发到各页
- 页面渲染：`renderOverview`(394) / `renderCars`(482) / `renderTech`(583) / `renderCompare`(654)
- 弹窗：`openCar`(725) / `openTech`(802) / `closeModal`(797)；轮播 startCarousel(837)
- 详情参数块：`specBlock(label,value,id,field)`(87)，值为 `—`/空时整块自动隐藏
- 编辑模式：`editable()`(72) 输出 contenteditable 文本；改动存 localStorage（`getOverride/setOverride`，按 state.reportMonth 隔离）；`exportDataJS()`(224) 导出合并后的 data.js
- 月份切换：`loadMonthDataset(month)`(902) 动态 fetch `js/data-YYYYMM.js`
- 图片：`imgOrPh(c,alt)`(339) 用**字符串拼接**生成 img（禁止改回模板字符串嵌套引号，曾导致属性截断、HTML泄漏的严重bug）；`escAttr()`(339) 转义；加载失败走全局 `window.IMG_ERROR_PH`(347)
- 价格清洗：`cleanPrice(p)`(351)，去掉"限时先享/补贴价/权益后"等啰嗦前缀，如"限时先享12.99万起（指导价13.99-19.59万）"→"12.99万起"
- `highlightNumbers`(19)：当前已关闭自动标红（只剥离标签，不注入新高亮），不要再打开
- 筛选器规则：板块 chip 7 个**全部显示**（含本月无覆盖的）；维度 chip 在选中板块后展开**该板块全部维度**，无覆盖的半透明并带"（无）"后缀；空结果显示"本月该维度无车型/技术"而非空白

## 五、data.js 数据格式

### 常量
- `MONTHLY_POOL`：月份池数组 `{month:'2026-09', label, file:'js/data.js', isCurrent:true}`，新增月份在此登记并加一个 data-YYYYMM.js
- `DOMAINS`：**26 个维度**，`{key, name, group}`，按 SE1~SE7 排序（见第六节）
- `DOMAIN_GROUPS`：7 板块 `{key:'se1', name, short:'SE1', color}`。板块色：SE1金#e8b830 / SE2青#35d0e0 / SE3红#ff6b4a / SE4暗红#e84545 / SE5绿#52c878 / SE6紫#a878f0 / SE7棕#c8a878
- `CALIBER`：口径映射 cert(认证)/official(官方)/media(媒体)/excel(竞品库)

### CARS 单条车型字段
```
id, name, brand, power(纯电/插混/增程/燃油), seg(级别), price, launchDate,
range, adas, cockpit, caliber,
usp: [卖点1,卖点2,...],          // 未上市但官网有预售卖点也必须填；只有确实无任何官方披露才留空
desc(简洁描述), 
src/srcUrl(USP来源名+链接), specsSrc/specsUrl(参数来源名+链接，汽车之家用真实数字车系ID：config/series/{id}.html，严禁用拼音slug或字母索引页),
featured: true/false, featuredNote(入选/落选理由),
dims: [覆盖的维度key],
specs: { size, power(支持\n多行，用"（1）…\n（2）…"), accel, topSpeed, weight,
         fastCharge, suspension, safety },  // 11块顺序：指导价/续航/快充/智驾/座舱/尺寸/动力/性能/质量/悬架/安全；空值自动隐藏
img: 'img/xxx.jpg'               // 编辑 specs 块时严禁误删本行
```

### TECHS 单条技术字段
```
id, date('MM-DD'), company, domain(维度key), title(公司+动作+技术名),
point(正文，句式见第六节), params(一行量化摘要),
caliber, impact(high/medium/low), impactNote(对我方启示),
metrics: [{label, value, compare?, direction?}],  // direction: up/down
src, srcUrl
```
技术条目按 date 升序排列。纯车型预售事件无实质新技术的不入 TECHS。

### 其他
- `TRENDS`：当月趋势归纳；`COMP_CARS`：竞品参数库（specs 大而全 + reviews 口碑 + uspGroups 分组卖点，caliber='excel'）；`PROJECTS`：竞品分组
- 9月数据真实校验结论示例（改动时勿回退）：N8L 车系ID=8159、EC6=5569、TT=8658、战舰700=8131、悦意07=7978、越7=8627、i60=8245、AT7=8702、MEGA=6939、N70=8731

## 六、业务规则（硬约束，不可违反）

1. **重点车型 featured 筛选**（二选一）：①改款/换代车型原车系月销曾超 4000 辆；②全新车型（非现有平台）。必须写 featuredNote 说明
2. **USP 逻辑**：未上市 ≠ 无卖点。官网预售页、车企官网、垂直媒体官方号上市通稿都算官方来源；只有"未上市且官方零披露"才空。填了 usp 必须同步 src/srcUrl
3. **来源口径**：官网/官方新闻稿/工信部公告 优先；汽车之家参数页与车家号官方通稿兜底；自媒体/论坛一律剔除或标"待验证"。价格直接写"xx万起"，禁止"限时先享""补贴价"等啰嗦话术
4. **技术 point 统一句式**："X月X日，[公司]在[场合]发布/披露[技术名]（[领域]）。采用[方案/架构]，[量化指标：提升/降低X%，带对比基准]。[前瞻意义/商业化节奏一句]。" 无官方量化的写"具体指标待官方技术文件验证"，**禁止编造数字**
5. **26 维度 7 板块**（key 必须与 DOMAINS 完全一致）：
   - SE1 新车吸引力(2)：styling 造型美学 / perceived 感知质量
   - SE2 使用舒适(5)：hmi 人机友好性 / airquality 空气质量 / thermal 热学舒适 / seat 座椅舒适性 / nvh NVH舒适性
   - SE3 动态行驶(3)：power 纵向动力性 / handling 动态操稳 / offroad 越野通过性
   - SE4 整车安全(3)：safeprevent 监控预防 / crash 碰撞保护 / antitheft 防盗防灾
   - SE5 节能经济(3)：energy 能耗与续航 / charging 充放电 / cost 用车成本
   - SE6 智舱智驾(3)：interaction 人机交互 / infotainment 娱乐互联 / adas 智能驾驶
   - SE7 适应环境(7)：durability 可靠耐久 / waterproof 防尘防水 / weathering 整车耐候性 / corrosion 抗腐蚀老化 / emc 电磁兼容性 / emissions 挥发和排放 / recycle 回收再循环
6. 图片禁止 AI 生成；无官方图就用"暂无图片"占位
7. 只改当月数据时往期文件（data-202606/07/08.js）不动

## 七、缓存版本号

index.html 底部两个 script 的 `?v=YYYYMMDDx`（当前 `20260918i`）。**每次改完 js/css 必须递增版本号**（如 20260920a），否则浏览器/CDN 用旧缓存用户看不到更新。

## 八、每月更新流程

1.（可选）双击 `一键备份.bat` 打快照
2. 改 `webapp/js/data.js`（或用网页编辑模式改完导出 data.js 覆盖）；新月份则新建 data-YYYYMM.js 并登记 MONTHLY_POOL，把旧当月文件改名归档
3. 递增 index.html 的 `?v=` 版本号
4. 本地双击 `启动网页服务.bat`，http://127.0.0.1:8765/ 验证（重点：console 零报错、筛选器、弹窗、图片不泄漏属性文本、来源链接可点）
5. 双击 `一键双推云端.bat` 推 Gitee+GitHub（依次粘贴两个 token；Gitee token 在 https://gitee.com/profile/personal_access_tokens 勾 projects 生成，GitHub token 在 https://github.com/settings/tokens/new 勾 repo 生成；只传变化文件，FAIL=0 即成功）
6. 如用 Netlify 手动部署：Netlify 后台 Production 区域拖 webapp 文件夹

## 九、部署状态（截至 2026-09-20）

| 渠道 | 地址/信息 | 状态与注意 |
|---|---|---|
| 本地 | http://127.0.0.1:8765/ | bat 启动，电脑开机+窗口在才可用 |
| 局域网 | http://10.4.149.98:8765/ | IP 是当时 DHCP 分配，换网会变，用 `ipconfig` 重查 |
| 飞书自建应用 | 名"特性集成AI月报"，App ID `cli_aa2649eda4f95cd8`，已发布 1.0.1，可用范围4人（张齐/范苡达/张世坤/李越） | 主页 URL 当前指向局域网地址；换公网地址后到开发者后台改 URL 并升版本发布 |
| Netlify | https://gleeful-begonia-e78e26.netlify.app/ | 已 claim 永久有效；**国内不挂代理打不开**，仅作国际备份 |
| Gitee | https://gitee.com/tsubakiou/competitor-intel | 代码已推；**Gitee Pages 需实名认证（身份证+手持，1-2工作日）后才能在"服务"菜单开启**，实名未完成 |
| GitHub | https://github.com/zq114514/competitor-intel | 代码已推、Pages 已开；国内访问不稳，仅备份 |

**国内公网托管未竟事项**：Gitee Pages（等实名审核）是首选；备选腾讯云 COS / 阿里云 OSS 静态网站托管（需公司有对应云账号，国内秒开）。定方案后把飞书应用主页 URL 改成最终国内公网地址。
**密钥管理**：GitHub/Gitee token 绝不写入任何项目文件；失效后让用户重新生成。

## 十、版本管理与回滚

- 本地：`versions/` 下每个快照都是完整可运行的 webapp。回滚 = 关闭服务 → 把快照内容复制覆盖 webapp/ → 重启服务
- 云端：Gitee/GitHub 保留全部提交历史，网页端可查看/下载任意历史版本
- 改任何大功能前，先双击 `一键备份.bat`

## 十一、已知坑（踩过，勿重蹈）

1. bat 含中文 → cmd GBK 乱码闪退。bat 只放 ASCII，中文逻辑放 ps1；ps1 用短路径（8.3）规避中文路径
2. img 用模板字符串+onerror 内联闭包 → 属性引号被截断、页面泄漏 `loading="lazy" onerror=...` 文本（严重bug）。现已改字符串拼接+escAttr+全局 IMG_ERROR_PH，勿回退
3. specsUrl 曾用猜的拼音 slug（/series/tt）和字母索引页（/grade/carhtml/B.html）冒充参数页 → 必须用真实数字车系ID
4. Python 后台进程由工具启动易被回收 → 让用户自己双击 bat 最稳
5. 浏览器 MCP 自动化偶发超时；截图在 tab 不可见时不可用；验证以 console 无报错+DOM 文本断言为准
6. 编辑 specs 对象时容易误删 `img` 行，每次 Edit 后检查

## 十二、新对话 AI 接手清单

1. 读本文件 + `monthly-workflow.md`
2. 读 `webapp/js/data.js` 头部（MONTHLY_POOL/DOMAINS/DOMAIN_GROUPS）确认维度体系
3. 改动前先启动本地服务看现状；大改前先备份
4. 严格执行第六节业务规则；所有数据要有官方来源，不确定标"待验证"
5. 改完递增缓存版本号 → 浏览器实测 console 零报错 → 再推云端
